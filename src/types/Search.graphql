type Search {
  id: String
  title: String
}

# type ProductList {
#   product: [Search]
# }

# extend type Query {
#   search: ProductList
# }

input ProductInput {
  query: String
}


extend type Query {
  search(input: ProductInput): [Search]
}
